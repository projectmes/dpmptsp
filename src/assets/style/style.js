import React from 'react';
import {StyleSheet, Dimensions} from 'react-native';

export const color = {
  lightColor: '#FBFBFF',
  darkColor: '#0D0106',
  errColor: '#FF331F',
  primaryColor: '#3b92f5',
  secondaryColor: '#5da4f5',
};

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: color.lightColor,
  },
});
